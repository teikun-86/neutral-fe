import AppLayout from "@/layouts/app";

const Cart = () => {
    return (
        <AppLayout title={`Cart ー ${process.env.NEXT_PUBLIC_APP_NAME}`}>
            
        </AppLayout>
    );
};

export default Cart;
import AppLayout from "@/layouts/app";

const Unfinish = () => {
    return (
        <AppLayout title="Unfinished Flight Booking">
            
        </AppLayout>
    );
};

export default Unfinish;